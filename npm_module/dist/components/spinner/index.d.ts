import React from 'react';
declare function Spinner({ color, radius, duration, strokeWidth, className, ...props }: {
    color?: string;
    radius?: number;
    duration?: number;
    strokeWidth?: string;
    className?: string;
}): React.JSX.Element;
export default Spinner;
