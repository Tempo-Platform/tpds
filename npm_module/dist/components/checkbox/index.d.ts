import React from 'react';
declare function Checkbox({ active, partial, disabled, onClick, ...props }: {
    active?: boolean;
    partial?: boolean;
    disabled?: boolean;
    onClick?: () => void;
}): React.JSX.Element;
export default Checkbox;
